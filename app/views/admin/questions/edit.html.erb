<h2><%= question_header(@question) %></h2>

<%= render 'form' %>
<p><%= button_to t('helpers.button.back'), admin_test_path(@question.test), method: :get, class: "btn btn-light text-custom border" %></p>
